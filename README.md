# RSA-Validility-of-public-key
An educational HTML and JavaScript tool that checks the validity of an RSA public key (n, e) using cryptographic rules. It factors n into primes, calculates φ(n), and verifies that gcd(e, φ(n)) = 1. Ideal for understanding RSA key generation, number theory, and modular arithmetic concepts.
